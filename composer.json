{
    "name": "venuno/module-order-import",
    "description": "Venuno destination-side contract for Magento 2. Exposes health, version and capabilities endpoints plus an idempotent, transactional order-import intake (POST /V1/venuno/orders/import) behind a Venuno token, which — when materialisation is enabled — creates native Magento sales orders.",
    "type": "magento2-module",
    "version": "0.3.0",
    "license": "OSL-3.0",
    "authors": [
        {
            "name": "Venuno",
            "homepage": "https://venuno.io"
        }
    ],
    "require": {
        "php": ">=8.1",
        "magento/framework": "^103.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.5"
    },
    "autoload": {
        "files": [
            "registration.php"
        ],
        "psr-4": {
            "Venuno\\OrderImport\\": ""
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Venuno\\OrderImport\\Test\\": "Test/"
        }
    },
    "scripts": {
        "test:unit": "phpunit --testsuite Unit"
    }
}
